# Agent tooling: vendored skill caches and machine-local settings.
# ~6.6MB / 300 files of installed packages, not project source.
.agents/
.claude/
.codex/
app/.agents/
app/.claude/
app/skills-lock.json

# OS noise
.DS_Store
Thumbs.db

# Obsidian per-machine UI state (open panes, window geometry) — churns constantly.
CrftdOS/.obsidian/workspace.json

# Handoff archive: every one of its 95 entries is already extracted and
# tracked under _import/interactive-flow-build-for-web-mobile-desktop/.
*-handoff.zip
