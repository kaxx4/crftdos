export default function KioskHome() {
  return (
    <div className="min-h-dvh flex items-center justify-center bg-ink text-cream font-mono text-sm text-center p-6">
      Design Studio kiosk (presets + canvas) is Phase 2 per PRD §15 — blocked on mockup images and print-area
      measurements (PRD §16.7–8). Kiosk PIN gate is live — this page is a stub behind it.
    </div>
  );
}
