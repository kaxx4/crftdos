export default function AdminHome() {
  return (
    <div className="min-h-dvh flex items-center justify-center bg-ink text-cream font-mono text-sm text-center p-6">
      Admin dashboard (analytics, pricing, B2B, catalogue, people, settings) is Phase 3–4 per PRD §15.
      <br />
      Admin PIN gate is live — this page is a stub behind it.
    </div>
  );
}
